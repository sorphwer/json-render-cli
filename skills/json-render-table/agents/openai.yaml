interface:
  display_name: "JSON Render Table"
  short_description: "Render compact data tables as images"
  default_prompt: "Render structured rows/columns into a compact table image using json-render-cli, with ticket table as a classic example."
