{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -f package.json ]; then npm install; else npm --prefix landingpage install; fi",
  "buildCommand": "if [ -f package.json ]; then npm run build; else npm --prefix landingpage run build; fi",
  "devCommand": "if [ -f package.json ]; then npm run dev; else npm --prefix landingpage run dev; fi"
}
